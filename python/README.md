# morfeusz2

Python bindings for Morfeusz 2. The module preserves the legacy `morfeusz2` API
shape while delegating analysis and generation to the Rust core library.

The Rust workspace also provides the core library, C ABI compatibility layer,
C++ source-compatibility header, CLI binaries, and JSONL service adapter.
